# 用户标签物料产品需求文档（PRD）

**版本**：V1.0  
**状态**：待评审  
**日期**：2026-07-15  
**涉及端**：iOS / Android / 服务端 / 运营后台  
**协作角色**：产品、视觉设计、客户端、服务端、测试、数据、运营

---

## 0. 决策摘要

本需求不是再增加一种“附加标签”，而是建设一套统一的**用户标识体系与标签物料能力**，解决当前各业务页面自行拼装、数量无上限、顺序不一致、奖励不可沉淀的问题。

本期建议落地以下产品决策：

1. **统一概念**：以“用户标识”作为总称，拆分为“系统徽章、资料胶囊、荣誉标签”三类；本次新增的可奖励物料正式命名为“荣誉标签”。
2. **三行分层**：完整资料场景固定为“等级身份行 / 资料属性行 / 荣誉标签行”。VIP、财富、魅力固定在同一行；新标签独立成行，不与性别、国家、语言混排。
3. **槽位而非堆叠**：高密度场景按槽位展示，聊天最多 3 个、列表主体最多 3 个、完整资料页每行最多 3 个；超出后隐藏或以“+N”进入标签墙。
4. **排序稳定**：上下文角色与官方身份优先，其次是 VIP 与当前场景相关等级，再次是场景标签和用户主动佩戴的荣誉标签；不得由接口返回顺序直接决定 UI 顺序。
5. **用户可佩戴**：用户拥有“我的标签”，最多佩戴 3 个荣誉标签并可拖动排序；系统身份、VIP、财富、魅力不可取消佩戴。
6. **服务端统一决策**：由服务端根据 scene_code 解析可展示标签，客户端只负责按统一组件渲染并执行兜底上限，避免各页面再次出现不同规则。
7. **分阶段迁移**：优先覆盖用户主页、房内资料卡、在线/排麦列表、搜索用户列表、公屏消息等核心场景；现有附加标签按兼容规则迁移，旧客户端继续可用。

### 0.1 一句话方案

> 把“标签”从散落在功能代码里的装饰图片，升级为可配置、可发放、可拥有、可佩戴、可过期、可统计，并能在不同场景稳定降级展示的用户数字资产。

---

## 1. 背景与问题

### 1.1 当前现状

客户端分析显示，平台当前已有 VIP、性别年龄、语言、国家、财富/魅力等级、附加标签、派对角色等多种标识，分布在 30+ 页面或组件中。其中：

- VIP、国家、财富/魅力等级覆盖面最广；
- 标签展示由不同业务组件分别实现；
- 附加标签虽可由服务端下发，但本质仍是“页面展示字段”，没有用户资产、发放记录、佩戴和有效期管理；
- 现有完整顺序大致为：派对角色 → VIP → 性别年龄 → 语言 → 国家 → 财富/魅力 → 附加标签；
- 在窄列表和聊天行中，多个小图标连续出现，昵称与核心操作空间被挤压。

附件截图中的“排队列表”即为典型高密度场景：头像、昵称、用户 ID、VIP、两个圆形等级标识以及操作按钮同时出现。继续直接追加新标签，会产生信息噪声、换行、按钮挤压和不同机型表现不一致等问题。

### 1.2 核心问题

| 问题 | 当前表现 | 业务影响 |
|---|---|---|
| 缺少独立物料 | 标签依附具体功能开发 | 无法作为奖励快速发放，活动复用成本高 |
| 缺少资产归属 | 只下发展示内容，不记录“用户拥有” | 无法做获得感、收藏、佩戴、过期提醒 |
| 缺少场景规则 | 页面各自决定显示哪些标签 | 顺序不一致，新增一次需多端多处改动 |
| 缺少数量上限 | 有多少展示多少或依赖自然裁剪 | 窄屏溢出、昵称被压缩、操作区不稳定 |
| 概念混杂 | 等级、属性、荣誉、角色都叫标签 | 产品、设计、研发难以建立统一规范 |
| 缺少降级机制 | 图片失败或旧版本不识别时不可控 | 空白、错位、旧端不可见 |
| 缺少数据闭环 | 无发放—佩戴—曝光—点击链路 | 无法评价奖励价值与标签质量 |

### 1.3 机会

统一标签物料可同时支撑：活动奖励、付费权益、粉丝/家族身份、官方认证、长期成就、新功能身份、榜单荣誉、限定纪念、召回激励等场景，并形成用户长期可积累的身份资产。

---

## 2. 目标与非目标

### 2.1 产品目标

1. 运营可在不发版的前提下创建、审核、上下线和发放荣誉标签。
2. 用户可查看已获得标签、佩戴最多 3 个并调整顺序。
3. 所有核心页面按同一套分类、槽位、上限、排序和溢出规则展示。
4. 新标签接入新功能时，只需配置可见场景，不再逐页定制逻辑。
5. 建立发放、佩戴、曝光、点击、到期的完整数据链路。

### 2.2 建议成功指标（上线后 30 天）

| 指标 | 建议目标 | 说明 |
|---|---:|---|
| 获得标签用户的标签页访问率 | ≥ 35% | 收到奖励后 7 日内访问“我的标签” |
| 获得标签用户的佩戴率 | ≥ 45% | 至少佩戴 1 个荣誉标签 |
| 核心场景规则覆盖率 | 100% | 不允许核心页面继续自行排序 |
| 标签展示溢出/错位缺陷 | 0 个 P0/P1 | 覆盖小屏、长昵称、多语言、RTL |
| 标签资源加载成功率 | ≥ 99.9% | 不含用户主动断网 |
| 标签解析接口 P95 | ≤ 100 ms | 建议走用户信息聚合接口并缓存 |

### 2.3 非目标

- 本期不重做 VIP、财富、魅力的等级计算与权益体系；
- 本期不开放用户自行上传标签图片或文字；
- 本期不建设用户间交易、赠送、合成、拆解标签；
- 本期不允许标签改变权限，权限仍由角色/权益服务判定；
- 本期不将性别、年龄、国家、语言改造成可领取资产。

---

## 3. 竞品与行业机制参考

> 说明：以下基于公开官方资料，重点参考产品机制而非逐像素复刻；不同地区、版本和账号状态下 UI 可能存在差异。

| 产品 | 公开机制 | 可借鉴点 | 本方案应用 |
|---|---|---|---|
| Twitch | 聊天徽章分全局、频道、活动等槽位；常规同时最多 3 个，特定预测徽章可形成第 4 个；同类徽章会互相覆盖，用户可选择展示身份 | 明确上限、槽位互斥、用户选择 | 公屏采用 3 槽位；同互斥组只留 1 个 |
| TikTok LIVE | 粉丝团等级通过观看、评论、送礼成长；会员徽章显示在直播用户名旁，并与入场聚光、等级庆祝等权益联动 | 标签与场景、成长、奖励绑定 | 支持 context_scope、成长等级、入场重点展示 |
| BIGO LIVE | 加入粉丝团后在聊天用户名旁获得带团名的徽章，升级后样式和特权变化；任务可发临时 VIP、入场效果；Family 身份在资料中展示且一个用户只能加入一个 Family | 有效期、成长样式、互斥关系、资产获得感 | 支持期限、level、mutual_exclusion_group、获得来源 |
| Discord | Profile Badges 汇集订阅、贡献、任务等多种身份，官方强调徽章与账号绑定、不可转移 | 资产归属与来源可信 | 标签实例绑定 user_id，保留发放审计，不可转移 |

### 3.1 行业结论

1. **聊天不是标签墙**：实时互动场景应优先保证昵称和消息可读，标签必须有硬上限。
2. **身份要有槽位**：角色、等级、荣誉不能仅按后台 priority 排成一串，否则每次活动都会破坏稳定识别。
3. **标签应有上下文**：房主、管理员、粉丝团、榜单冠军等只在相关场景更有意义。
4. **用户应有选择权**：非强制身份的荣誉标签应允许佩戴、排序和隐藏。
5. **完整展示应渐进展开**：主页显示核心标签，点击进入标签墙查看全部资产和获得方式。

### 3.2 专业判断

不建议把所有既有标识全部改造成一种通用标签，也不建议沿用“附加标签在现有标签行末尾无限追加”。最稳妥的架构是：

- 稳定的等级身份保持固定位置，保护付费和成长识别；
- 资料属性降级为辅助信息，不参与荣誉排序；
- 新增荣誉标签独立成行，给奖励与个性表达明确空间；
- 高密度场景使用槽位裁剪，完整信息通过资料卡/标签墙承接。

---

## 4. 名词与分类

### 4.1 总体分类

| 一级概念 | 二级类型 | 示例 | 是否资产 | 用户可否佩戴/隐藏 |
|---|---|---|---|---|
| 系统徽章 | 场景角色 | 房主、管理员、官方人员 | 否 | 不可 |
| 系统徽章 | 等级身份 | VIP、财富、魅力 | 否 | 不可；按场景裁剪 |
| 资料胶囊 | 基础属性 | 性别年龄、国家、语言 | 否 | 受资料与隐私设置控制 |
| 荣誉标签 | 永久荣誉 | 年度贡献者、首批用户 | 是 | 可佩戴/隐藏 |
| 荣誉标签 | 限时活动 | 节日冠军、活动 TOP10 | 是 | 可佩戴；到期自动卸下 |
| 荣誉标签 | 组织关系 | 公会/家族、粉丝团 | 是或关系映射 | 可配置；同组互斥 |
| 荣誉标签 | 功能身份 | 新功能体验官、游戏达人 | 是 | 可佩戴/隐藏 |
| 荣誉标签 | 官方认证 | 官方主播、签约艺人 | 是但强制展示 | 不可隐藏 |

### 4.2 展示形态

1. **Icon 徽章**：1:1，小空间使用；适合聊天、榜单、昵称前。
2. **Pill 标签**：图标 + 本地化文本；适合主页、资料卡、列表第二行。
3. **Level 徽章**：等级数值与固定主题；仅用于 VIP、财富、魅力或可成长标签。
4. **Animated 标签**：轻动效或跑马灯；仅允许资料页/入场横幅使用，高频列表与聊天禁用。

---

## 5. 信息架构与页面布局

### 5.1 完整资料场景：固定三行

适用于“我的”、用户主页、完整用户资料卡。

| 行 | 内容 | LTR 固定顺序 | 上限 | 溢出 |
|---|---|---|---:|---|
| 等级身份行 | 场景角色（如有）、VIP、财富、魅力 | 角色 → VIP → 财富 → 魅力 | 主页 3；房内资料卡 4 | 按场景优先级隐藏最低项，不出现 +N |
| 资料属性行 | 性别年龄、国家、语言 | 性别年龄 → 国家 → 语言 | 3 | 超出隐藏；不横向滚动 |
| 荣誉标签行 | 官方认证、场景标签、用户佩戴标签 | 强制 → 场景 → 佩戴顺序 | 3 | 显示“+N”，点击进入标签墙 |

**明确规则**：VIP、财富、魅力始终同属“等级身份行”；荣誉标签不得插入该行。RTL 环境视觉顺序整体镜像，但逻辑优先级不变。

### 5.2 高密度场景：固定槽位

高密度场景不再展示完整三行，而是从下列槽位中按页面预算取值：

| 槽位 | 内容 | 典型上限 | 互斥规则 |
|---|---|---:|---|
| S0 上下文身份 | 房主、管理员、官方/安全身份 | 1 | 同一上下文角色只展示最高角色 |
| S1 会员身份 | VIP | 1 | 同一 VIP 只展示当前最高等级 |
| S2 场景成长 | 与当前页面最相关的财富或魅力 | 1 | 榜单按榜单维度；其他场景按配置 |
| S3 荣誉表达 | 场景标签或用户佩戴标签 | 1 | 场景强相关优先于通用佩戴 |

页面预算小于槽位数时，按 S0 → S1 → S2 → S3 裁剪；但若运营将某荣誉标签配置为“强场景标签”，其进入 S0 之后、S1 之前，且必须经过产品审核。

---

## 6. 全场景展示上限与排序

### 6.1 场景矩阵

下表中的“上限”均指同一用户区域**首屏可见**的标识数量；“荣誉”指本次新增的荣誉标签。相同复用组件必须使用同一规则。

| # | 场景组（覆盖页面） | 布局 | 总上限 | 其中荣誉上限 | 排序 | 超出处理 |
|---:|---|---|---:|---:|---|---|
| 1 | “我的”页、用户主页（个人空间） | 三行 | 9（3+3+3） | 3 | 等级固定；资料固定；荣誉按 7.3 | 荣誉显示 +N，进入标签墙 |
| 2 | 房内用户资料卡、房间信息卡（详情/关注） | 三行紧凑版 | 9（4+2+3） | 3 | 角色 → VIP → 财富 → 魅力；资料取国家/语言；荣誉按 7.3 | 荣誉 +N；资料超出隐藏 |
| 3 | 在线用户、邀请用户、邀请上麦、管理员、排麦申请列表 | 昵称行 + 单标签行 | 3 | 1 | 角色 → VIP → 场景成长 → 荣誉 → 另一成长 | 直接隐藏；点击用户看完整资料 |
| 4 | 搜索结果、黑名单、访客、通用用户卡片 | 两行 | 5（标识 3 + 资料 2） | 1 | VIP → 场景成长 → 荣誉；资料为国家 → 语言/性别年龄 | 直接隐藏；不显示 +N |
| 5 | 消息会话列表 | 单行紧凑 | 2 | 1 | VIP → 强场景/官方荣誉 → 用户佩戴荣誉 | 直接隐藏；国家等移出昵称行 |
| 6 | 私聊顶部、视频通话等待/结束页 | 两行 | 5（等级 3 + 资料/荣誉 2） | 1 | VIP → 财富 → 魅力；官方/佩戴荣誉 → 国家 | 直接隐藏 |
| 7 | 社区动态发布者区 | 单行 | 3 | 1 | VIP → 场景成长 → 荣誉 | 直接隐藏；属性在个人资料查看 |
| 8 | 房间公屏 7 类消息、弹幕、群聊消息 | 昵称前槽位 | 3 | 1 | 角色 → VIP → 场景成长 → 强场景/佩戴荣誉 | 到达上限即隐藏；禁止 +N、滚动、换行 |
| 9 | 用户进场特效横幅、等级庆祝 | 重点展示 | 4 | 1 | 当前触发标签 → VIP → 财富 → 魅力 | 触发标签必须展示；其余按预算隐藏 |
| 10 | 全站榜单、用户榜单、房间榜单（头部与列表） | 榜单专用 | 2 | 0（冠军荣誉可替换为 1） | VIP → 当前榜单维度；榜单荣誉仅在活动配置时替换第二项 | 直接隐藏，避免与名次抢视觉 |
| 11 | 派对房列表、大卡片、发现搜索房间、游戏 Tab | 房间标签行 | 房间标签 3；房主标识另计 1 | 房间荣誉 1 | 房间状态/玩法 → 语言 → 国家；房主仅 VIP | 直接隐藏；禁止展示房主全部个人标签 |
| 12 | VIP 中心、等级页、礼物等级门槛、派对表情门槛、商店数据 | 功能说明 | 1 | 0 | 只展示与当前门槛/权益直接相关的等级 | 不补充其他标签 |
| 13 | 账号信息页 | 表单行 | 每项 1 | 0 | 国家使用“国旗 + 国家名”；新增“我的标签”入口 | 不做标签拼接 |

### 6.2 对附件“排队列表”的落地

建议保持现有行高，不新增第三行。昵称下方/旁边最多展示 3 个标识：

1. 有房间角色时先展示角色；
2. 展示 VIP；
3. 在财富、魅力、强场景荣誉标签中按场景优先级取 1 个；
4. 无角色时可再补 1 个，仍不得超过 3 个；
5. “同意/拒绝”操作区宽度固定，不允许标签侵占；长昵称优先省略，标签行不得横向滚动。

### 6.3 特殊场景

- **礼物面板等级角标**是“使用门槛”，不是用户已拥有标签，必须与用户标签组件分离。
- **账号国家文本标签**属于资料字段，不进入荣誉标签体系。
- **跑马灯标签**在聊天、会话列表、榜单、排麦等高频场景强制降级为静态 Icon/Pill。
- **官方/安全身份**如客服、审核员必须优先展示且不可由用户隐藏；点击后展示官方说明，降低仿冒风险。

---

## 7. 统一排序、互斥与溢出算法

### 7.1 处理顺序

服务端针对 scene_code 按以下步骤生成展示结果：

1. **有效性过滤**：未开始、已过期、已撤回、不可见地区、版本不支持、用户隐私关闭的标签不进入候选。
2. **互斥去重**：相同 template_id、业务唯一键或 mutual_exclusion_group 只保留一个。
3. **强制身份入槽**：上下文角色、官方/安全身份进入对应槽位。
4. **场景相关性计算**：当前榜单、房间、活动、功能相关标签优先。
5. **用户佩戴计算**：读取用户佩戴状态和 position 1~3。
6. **稳定排序**：按类型与排序键排序。
7. **场景裁剪**：根据场景矩阵的总上限与分类型上限裁剪。
8. **客户端兜底**：客户端再次执行 hard_cap，防止错误配置撑破布局。

### 7.2 跨类型优先级

默认从高到低：

1. 上下文角色（房主、管理员）
2. 官方/安全强制身份
3. VIP
4. 当前场景相关的财富或魅力等级
5. 当前场景强相关荣誉标签
6. 另一成长等级
7. 用户佩戴的通用荣誉标签
8. 系统自动推荐的未佩戴荣誉标签（仅完整资料页可用）
9. 资料属性（只在资料槽位内排序，不与上述类型竞争）

**例外**：在“标签获得/等级庆祝/入场效果”中，本次触发的标签提升为第一优先级；在财富榜/魅力榜中，对应等级固定优先于通用荣誉标签。

### 7.3 荣誉标签内部排序

荣誉标签使用以下稳定排序键，前项相同才比较后一项：

1. `forced_display`：强制 > 非强制；
2. `context_match_score`：精确场景 > 同业务域 > 全局；
3. `equipped`：已佩戴 > 未佩戴；
4. `equip_position`：1 → 2 → 3；
5. `rarity`：传奇 S > 稀有 A > 进阶 B > 普通 C；
6. `display_priority`：数值越大越优先；
7. `acquired_at`：最近获得优先；
8. `tag_instance_id`：升序，保证多端结果稳定。

### 7.4 互斥规则

- 同一个粉丝团/家族等级只展示最高等级；
- 同一活动的阶段标签只展示最高阶段；
- 同一认证组只展示可信度最高的一个；
- 同一用户不得同时展示相互冲突的房间角色；
- 标签升级默认替换旧实例，除非运营明确配置“旧版可收藏但不可佩戴”；
- 已过期标签自动卸下，后续佩戴位前移补齐，不自动佩戴新标签。

### 7.5 溢出原则

1. 高密度场景：直接隐藏，不横向滚动、不换行、不出现 +N。
2. 完整资料页：荣誉行显示最多 3 个及“+N”；+N 不计入标签数量，但占固定 28dp 宽度。
3. 标签墙：展示全部，按“已佩戴 / 即将到期 / 永久 / 已过期”分组。
4. 服务端未返回排序字段时，客户端按 type 默认优先级 + tag_id 兜底，严禁依赖数组原始顺序。

---

## 8. 用户侧功能

### 8.1 “我的标签”入口

入口建议：

- 我的页面：标签行“+N”或右侧“管理”；
- 账号信息/装扮中心：新增“我的标签”；
- 获得标签弹窗：主按钮“立即佩戴”，次按钮“查看标签”。

### 8.2 标签墙

分组与信息：

- **已佩戴**：最多 3 个，可拖动排序；
- **全部拥有**：展示稀有度、名称、有效期、来源；
- **即将到期**：剩余 ≤ 7 天时单独提示；
- **已过期**：默认折叠，可查看历史，不可佩戴；
- 标签详情：大图、名称、描述、获得时间、有效期、获得来源、适用场景、当前佩戴状态；可配置跳转“去完成/去查看活动”。

### 8.3 佩戴规则

1. 最多佩戴 3 个非强制荣誉标签；
2. 点击“佩戴”时有空位则放入最后一位；已满时进入替换选择；
3. 支持拖动调整 1~3 位顺序；
4. 强制官方标签不占 3 个用户佩戴位，但占页面荣誉展示预算；
5. 限时标签显示到期时间；到期自动卸下；
6. 用户可隐藏普通荣誉标签；官方/安全身份不可隐藏；
7. MVP 使用一套全局佩戴顺序，后续版本再开放“聊天/主页分别佩戴”。

### 8.4 获得反馈

- 单次获得：弹窗/底部卡片展示标签、有效期与“立即佩戴”；
- 批量获得：消息中心通知 + 标签墙红点，不连续弹多个弹窗；
- 标签升级：使用“已升级至 Lv.X”，默认替换旧展示；
- 已拥有重复标签：按模板配置执行延长有效期、提升等级或忽略，并明确反馈；
- 被撤回：仅在违规、误发等情况下发生，写入系统通知与审计原因。

---

## 9. 运营后台与发放能力

### 9.1 标签模板管理

后台需支持：新建、复制、预览、提交审核、定时上线、暂停发放、下线展示、归档。

必填配置：

| 字段 | 说明 |
|---|---|
| tag_template_id | 全局唯一，不复用 |
| 内部名称 / 展示名称 | 内部检索名与多语言用户可见名分离 |
| 类型 / 子类型 | 荣誉、活动、组织、功能、认证等 |
| 稀有度 | S/A/B/C，仅影响视觉与同级排序，不直接代表权益 |
| 资源包 | Icon、Pill 背景/图标、深浅色资源、动效可选 |
| 生效与失效时间 | 模板生命周期；与用户实例有效期分离 |
| 用户实例期限 | 永久、固定到期、领取后 N 天 |
| 可见场景 | scene_code 白名单；默认仅主页/资料卡 |
| 展示优先级 | 0~1000；仅同类型内生效 |
| 互斥组 | 如 family、fan_club_x、activity_stage_x |
| 强制展示 / 可隐藏 | 强制仅限官方、安全、强场景标签 |
| 点击行为 | 无、打开详情、内部路由、活动页 |
| 最低客户端版本 | 用于新样式与降级控制 |
| 运营说明 | 获得规则、撤回规则、客服口径 |

### 9.2 发放方式

- 单用户发放；
- 用户 ID 批量导入；
- 人群包发放；
- 任务/活动系统实时发放；
- 接口发放（必须有幂等键）；
- 补发与撤回。

每次发放记录：operator、source_type、source_id、reason、idempotency_key、issued_at、expire_at、before/after 状态。批量发放需支持预估人数、二次确认、进度、成功/失败明细和停止任务。

### 9.3 审核与权限

- 素材创建与发放权限分离；
- 官方认证类需更高权限和双人审核；
- 上线后修改视觉资源需产生新版本并保留历史；
- 已发放标签不可直接删除模板，只能停用/撤回；
- 所有批量操作保留审计日志与导出能力。

---

## 10. 视觉与资源规范

### 10.1 尺寸规范

| 形态 | 使用场景 | 展示高度建议 | 最大展示宽度 | 源资源建议 |
|---|---|---:|---:|---|
| Icon | 公屏、榜单、昵称前 | 14/16/18dp | 等高 | 72×72 px，PNG/WebP |
| Compact Pill | 列表、资料卡 | 18dp | 64dp | 图标 + 程序化文本优先 |
| Standard Pill | 主页、标签墙 | 20dp | 80dp | 背景可拉伸，文本不写死在图片中 |
| Emphasis Pill | 入场、获得弹窗 | 24dp | 112dp | 可含轻动效，必须有静态降级图 |

### 10.2 设计约束

1. 标签名称建议中文 ≤ 6 个汉字；拉丁/阿拉伯文字按 12 个可见字符设计，以最大宽度和省略号为最终约束。
2. 文本必须使用本地化字段，不将名称直接烘焙进图片；特殊艺术字需同时提供无文字底图和本地化方案。
3. 单资源建议 ≤ 60KB；动效建议 ≤ 200KB、时长 ≤ 2 秒、循环间隔 ≥ 3 秒。
4. 高频滚动场景只允许静态资源；同时可见动效标签最多 1 个。
5. 标签不能只依赖颜色表达等级；图形、文本或纹理至少再提供一种区分。
6. 深色/浅色背景均需清晰；文本对比度建议达到 4.5:1，非文本关键图形建议达到 3:1。
7. 保持 2dp 标签间距；标签与昵称间距 4dp；不得使用负间距叠压。
8. 标签加载失败时显示稳定宽度的通用占位图标或直接隐藏，不显示破图与原始 URL。

### 10.3 多语言与 RTL

- 逻辑顺序不变，视觉排列在 RTL 下整体镜像；
- 数字等级保持自然数字方向，不随图标倒置；
- 阿拉伯语文本使用本地化字体并测试连写；
- 不允许通过反转整张图片实现 RTL，方向性箭头需提供 RTL 资源；
- 省略号位置遵循系统文本方向。

---

## 11. 数据模型与接口要求

### 11.1 核心对象

**TagTemplate（标签模板）**

- 描述一个可发放的标签是什么、长什么样、在哪展示、如何互斥；
- 模板不可转移，不直接代表某个用户已拥有。

**UserTagInstance（用户标签实例）**

- 描述某用户何时、因何获得模板，是否佩戴，何时到期；
- 唯一键建议为 `user_id + template_id + business_instance_key`。

**TagDisplayResult（场景展示结果）**

- 服务端按 `user_id + scene_code + context_id + app_version + locale` 计算；
- 返回已排序、已互斥、已裁剪的展示结果及槽位信息。

### 11.2 建议返回字段

| 字段 | 用途 |
|---|---|
| instance_id / template_id | 追踪实例与模板 |
| type / subtype | 客户端兜底分类 |
| slot | S0/S1/S2/S3 或 PROFILE_LEVEL/PROFILE_INFO/PROFILE_HONOR |
| display_order | 当前场景最终顺序 |
| render_mode | icon / pill / level / animated |
| icon_url / background_url / fallback_url | 资源与降级 |
| display_name / accessibility_label | 本地化名称与读屏文本 |
| level / rarity | 等级与稀有度 |
| click_action | 点击行为白名单 |
| expire_at / remaining_seconds | 到期展示 |
| forced / hideable | 是否强制与可隐藏 |
| analytics_token | 曝光点击归因 |

### 11.3 接口原则

1. 场景代码统一枚举，禁止页面传自由文本；
2. 服务端结果与用户基础信息聚合返回，避免列表 N+1 请求；
3. 客户端按用户 + 场景缓存，用户佩戴/获得/到期后主动失效；
4. 发放接口必须幂等，批量任务可断点续跑；
5. CDN 资源带版本哈希并支持预热；
6. 服务端配置异常时使用最近一次成功配置；完全不可用时仅展示本地系统徽章；
7. 权限判断不得依赖标签是否成功渲染。

### 11.4 旧版本兼容

- 新服务端同时保留现有 `additional_tags` 兼容字段至少 2 个客户端大版本；
- 对旧端仅投影第 1 个可兼容静态荣誉标签，不下发动画、点击路由和 +N；
- 新端优先读取 `tag_display_result`，不得同时重复渲染旧字段；
- 最低版本不满足时自动使用 fallback_url 或隐藏；
- 迁移期埋点区分 legacy_renderer 与 unified_renderer。

---

## 12. 埋点与数据看板

### 12.1 事件

| 事件 | 关键参数 |
|---|---|
| tag_granted | template_id、source、duration、batch_id |
| tag_grant_result | success/error_code、idempotency_key |
| tag_inventory_view | entry_source、owned_count、equipped_count |
| tag_equip / tag_unequip / tag_reorder | instance_id、from/to_position |
| tag_exposure | scene_code、slot、display_order、renderer、analytics_token |
| tag_click | scene_code、click_action、instance_id |
| tag_expired | instance_id、was_equipped |
| tag_overflow | scene_code、candidate_count、display_count、hidden_types |
| tag_asset_load | resource_version、success、latency、fallback_used |

### 12.2 看板

- 发放：人数、成功率、来源、重复/失败原因；
- 资产：人均拥有数、有效/过期数、稀有度分布；
- 使用：佩戴率、佩戴顺序、7/30 日留存；
- 展示：分场景曝光、点击、溢出率；
- 质量：资源失败率、解析耗时、旧端占比、崩溃/卡顿；
- 运营：单标签获得—佩戴—曝光—点击漏斗。

---

## 13. 异常、边界与风险控制

| 场景 | 处理规则 |
|---|---|
| 标签刚过期但客户端离线 | 本地依据 expire_at 立即隐藏；联网后同步 |
| 同一标签重复发放 | 按模板策略延长/升级/忽略；接口返回明确结果 |
| 用户同时在多端排序 | 服务端版本号乐观锁；后提交覆盖并提示刷新 |
| 素材 URL 失效 | 走 fallback；上报失败；连续失败触发后台告警 |
| 后台误配 20 个强制标签 | 服务端仍执行场景 hard_cap；强制不等于突破上限 |
| 长昵称 + 3 标签 + RTL | 昵称省略，操作区与标签槽位宽度固定；不换行 |
| 用户被封禁/注销 | 资产不可展示；按账号治理规则保留审计 |
| 标签被撤回 | 全端缓存失效；已佩戴位前移；消息中心说明 |
| 敏感/仿冒视觉 | 官方标签使用平台保留样式；普通标签禁用近似官方图形 |
| 低端机性能 | 禁用列表动效；资源懒加载、内存缓存设上限 |

### 13.1 主要风险

1. **商业等级曝光下降**：通过固定等级身份行、榜单相关等级优先、上线前 A/B 对比控制。
2. **运营滥用强制展示**：强制标签设高权限、双人审核、总量告警和页面硬上限。
3. **客户端迁移不彻底**：建立组件接入清单和代码扫描，禁止新页面直接消费原始标签数组。
4. **标签价值通胀**：限定稀有度发放规模，永久标签慎用，后台展示预计覆盖人数。
5. **用户不知道来源**：所有荣誉标签必须有详情、获得来源和有效期。

---

## 14. 上线范围、迁移与灰度

### 14.1 MVP 范围

1. 标签模板、用户实例、发放/撤回、有效期、互斥；
2. 我的标签：查看、佩戴 3 个、排序、卸下；
3. 统一服务端场景解析与客户端渲染组件；
4. 覆盖场景：我的/用户主页、房内资料卡、在线/邀请/排麦列表、搜索用户、公屏消息、私聊顶部；
5. 运营后台模板管理、单发/批量发放、审核与审计；
6. 现有附加标签兼容迁移；
7. 完整埋点与基础看板。

### 14.2 后续版本

- 主页/聊天分场景佩戴；
- 标签成长与升级动画；
- 标签组合套装、收藏册；
- 标签获得任务入口；
- 房间标签资产与用户标签资产完全统一；
- 个性化推荐与标签搜索筛选。

### 14.3 迁移步骤

1. 盘点现有附加标签模板、使用页面、有效期和业务负责人；
2. 将普通图片标签映射为 TagTemplate；跑马灯补静态 fallback；
3. VIP、财富、魅力、角色保持原业务源，但通过统一展示结果输出；
4. 新旧组件双读比对，不对用户重复展示；
5. 核心页面逐个切换统一渲染器；
6. 停止新增旧 additional_tags 接入；
7. 旧版本占比达标后下线兼容字段。

### 14.4 灰度策略

- 内部/测试账号：全量规则验证；
- 5% 用户：仅主页与资料卡；
- 20% 用户：增加列表和公屏；
- 50% 用户：开放运营发放；
- 100%：迁移完成后关闭旧渲染；
- 每阶段观察至少 24 小时，重点关注崩溃率、标签资源失败率、昵称可读性投诉、公屏帧率与商业等级曝光。

需提供服务端总开关、scene_code 开关、单模板停用开关和客户端统一组件降级开关。

---

## 15. 验收标准

### 15.1 功能验收

- [ ] 运营可创建永久/限时标签，配置多语言、资源、场景、互斥与优先级；
- [ ] 支持单发、批量、人群、接口发放，重复请求不重复生成资产；
- [ ] 用户可查看全部拥有标签，最多佩戴 3 个并调整顺序；
- [ ] 到期标签自动隐藏与卸下，标签墙保留过期记录；
- [ ] 完整资料页严格按三行展示，VIP/财富/魅力同一行；
- [ ] 每个场景严格执行第 6 章上限，不因强制配置突破；
- [ ] 相同用户、相同场景、相同数据在 iOS/Android 顺序一致；
- [ ] 点击标签可查看详情，官方标签可验证身份；
- [ ] 旧端只展示兼容静态标签且不重复。

### 15.2 视觉与兼容验收

- [ ] 覆盖 320dp 小屏、系统最大字体、最长昵称、3 个标签同时出现；
- [ ] 覆盖中文、英文、阿拉伯语和 RTL；
- [ ] 标签不得挤压固定操作按钮，不换行、不遮挡；
- [ ] 高密度场景无跑马灯和循环动效；
- [ ] 深浅色背景、资源失败、弱网、离线均有可接受降级；
- [ ] 读屏能读出标签名称和等级，不仅播报“图片”。

### 15.3 性能验收

- [ ] 列表首屏不产生按用户逐条请求；
- [ ] 100 人房间列表滚动无明显掉帧；
- [ ] 公屏连续消息不因标签解码产生主线程卡顿；
- [ ] 服务端解析 P95 ≤ 100ms，资源加载成功率 ≥ 99.9%；
- [ ] 标签缓存可在发放、佩戴、撤回、到期后正确失效。

---

## 16. 评审待确认项

以下不影响总体架构，但需在 UI/技术评审中定稿：

1. 荣誉标签稀有度是否面向用户展示，还是仅用于视觉和后台排序；
2. 黑名单等治理列表是否继续展示 VIP/等级，建议仅保留最多 1 个必要身份；
3. 公屏 S3 荣誉槽是否默认开放，还是仅对“强场景标签”开放；
4. 现有男女财富/魅力展示逻辑是否长期保留双标签，建议本期不改计算，仅按场景裁剪；
5. 标签到期是否发送 Push，建议 MVP 仅站内提醒，避免打扰；
6. 标签墙是否进入现有装扮中心，取决于当前信息架构与研发复用成本。

---

## 附录 A：建议 scene_code

`PROFILE_SELF`、`PROFILE_OTHER`、`ROOM_USER_CARD`、`ROOM_QUEUE_LIST`、`ROOM_ONLINE_LIST`、`ROOM_INVITE_LIST`、`USER_SEARCH_LIST`、`VISITOR_LIST`、`CONVERSATION_LIST`、`PRIVATE_CHAT_HEADER`、`COMMUNITY_FEED`、`ROOM_PUBLIC_CHAT`、`ROOM_DANMAKU`、`GROUP_CHAT`、`ROOM_ENTRY_BANNER`、`GLOBAL_RANK`、`ROOM_RANK`、`ROOM_CARD`、`CALL_WAITING`、`CALL_RESULT`、`TAG_INVENTORY`。

## 附录 B：公开参考资料

1. Twitch Help，《How to Use Badges》：https://help.twitch.tv/s/article/how-to-use-badges  
2. Twitch Help，《Twitch Chat Badges Guide》：https://help.twitch.tv/s/article/twitch-chat-badges-guide  
3. TikTok Support，《LIVE Fan Club》：https://support.tiktok.com/en/live-gifts-wallet/tiktok-live/live-fan-club  
4. BIGO LIVE Official Blog，《Bigo Live App Features》：https://www.bigo.tv/blog/bigo-live-app-features  
5. BIGO LIVE Official Blog，《How to Start or Join a Family On BIGO LIVE》：https://blog.bigo.tv/faq/bigo-family/  
6. Discord Support，《Profile Badges 101》：https://support.discord.com/hc/en-us/articles/360035962891-Profile-Badges-101  

检索日期：2026-07-15。

## 附录 C：现状材料

- 《用户标签控件使用分析.md》：客户端现有标签类型、展示位置与组合规律；
- 附件截图：派对房排队列表，展示高密度用户行中 VIP 与圆形等级标签的现状。

